# Sampling interval in server ticks (20 ticks = 1 second).
sampling:
  interval-ticks: 100

output:
  # Output folder for parquet files. Relative paths are resolved against the plugin data folder.
  path: data

logging:
  positions: true
  deaths: true
  spawns: true
  wool: true
